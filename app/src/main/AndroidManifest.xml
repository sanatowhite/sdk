<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Phase 4 会加 <application android:name=".ProbeApp"> 等真实内容，
         这里先留空壳让 Phase 3 的模块拓扑能编译起来。 -->
    <application android:label="@string/app_name" />
</manifest>
