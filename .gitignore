*.iml
.gradle
/local.properties
.idea/
.DS_Store
# 通配而不是逐模块硬编码：新增模块（core-*/app/debug-tools/benchmark/...）
# 不需要每次都来加一行。
**/build/
.kotlin/

# scripts/logkit-keygen.sh 的输出——真正的生产私钥绝不能被提交。这些模式刻意
# 不匹配 logkit/keys/debug-private-key.pem(那份是刻意签入的 THROWAWAY 测试
# 密钥,固定路径,不受这里的通配影响,见其旁边的 README 说明)。
logkit-private-*.pem
logkit-public-*.der
/logkit-keys/

# 真实 Firebase 项目配置，只本地自测用——仓库里 app/google-services.json 必须保持占位，
# 见该文件里的 _comment 说明。
/.local/
