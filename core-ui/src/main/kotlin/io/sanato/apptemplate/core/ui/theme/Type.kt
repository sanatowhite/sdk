package io.sanato.apptemplate.core.ui.theme

import androidx.compose.material3.Typography

// M3 默认 Typography() 已经是一套完整、可读的排版尺度——
// 只有 fork 出去的项目要换自定义字体时才需要在这里覆盖具体 TextStyle。
internal val AppTypography = Typography()
